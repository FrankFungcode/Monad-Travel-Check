# API Configuration
VITE_API_BASE_URL=http://localhost:3000

# Red Packet Signer Private Key (for signature generation)
# WARNING: This key will be exposed in the frontend bundle!
# Only use a dedicated key for red packet signing, NOT your main wallet.
# This should match the signer address set in the deployed contract.
VITE_SIGNER_PRIVATE_KEY=your_signer_private_key_here
